# 2026-08-05

## 主页重构：视觉杂志风（替换云优桌面）
- 用户觉得原 Web OS 桌面样式"无聊"，要求设计其他风格个人主页，选定「视觉杂志风」
- 新建 `src/components/home/MagazineHome.vue`：
  - 深炭黑背景 + 漂移光晕 + 噪点纹理
  - 12 列非对称杂志网格（hero 封面卡 7×4 + 8 应用卡 + 引言卡 + 底部小卡）
  - 每张应用卡独立渐变配色 + 杂志编号 + hover 上浮/图标动效
  - 滚动浮现动画（IntersectionObserver + staggered delay）
  - 保留窗口系统：点卡片用 windowStore.openWindow 弹出应用窗口
  - 聊天卡跳转 /chat 路由；GitHub 卡外链
  - 移动端降级为两列网格
- 路由调整：`/` → MagazineHome（fullScreen，不显示任务栏）；原桌面保留为 `/desktop` 入口
- Window.vue 根元素加 `pointer-events-auto`（配合杂志页窗口层 pointer-events-none 容器）
- index.html 标题改为「云优网络 · YUNYOU」
- 生产构建通过；dev server 5174 端口

## 清理死代码：移除 Desktop 模块组
- 删除 `Desktop.vue`、`DesktopIcon.vue`、`ContextMenu.vue`、`Taskbar.vue`
- `App.vue` 精简为纯 `<router-view />`，移除 Taskbar + presenceStore 引用
- `router/index.js` 移除 `/desktop` 路由和 Desktop import
- 保留 `Window.vue`（杂志页仍用）、`@vueuse/core`（Window.vue 的 useDraggable 依赖）
- `presenceStore.initPresence()` 重复调用已消除（原 App.vue + MagazineHome 各一次，现仅 MagazineHome）
- 主包：153 kB → 140 kB
- 新增 README.md（项目介绍+快速开始）、CHANGELOG.md（2025-08-05 改动记录）、LICENSE（MIT）

## CLAUDE.md 项目文档
- 创建根目录 `CLAUDE.md`，覆盖项目概述、技术栈、命令、文件结构、路由/窗口/壁纸/AI 代理架构说明、环境变量、注意事项
- 问题：reveal 动画初始 `opacity: 0` + `IntersectionObserver` 未触发，导致首屏下方卡片完全透明
- 修复 `src/components/home/MagazineHome.vue`：
  - `.reveal` 默认状态改为 `opacity: 0.82` + `translateY(14px) scale(0.985)`，保证即使 observer 失败也能看见内容
  - IntersectionObserver 改为在 `requestAnimationFrame` 中初始化，threshold 降低为 0.05 并加 rootMargin
  - 添加 `prefers-reduced-motion` 媒体查询兜底
- 重新截图验证：hero + 8 应用卡 + 引言卡 + 底部小卡全部正常显示

## 其他
- 提交推送过一轮 git（rebase 本地 ignore 提交到远程 main 之上）
- 排查过 playwright-cli 浏览器崩溃问题（沙箱环境），最终用 playwright-core + 缓存 chromium 写脚本截图成功
